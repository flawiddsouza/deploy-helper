- name: Successful deployment with cleanup
  hosts: test
  tasks:
    - name: Complete the deployment
      shell: echo main_succeeded

  on_failure:
    - name: On-failure task should not run
      shell: echo on_failure_should_not_run

  always:
    - name: Clean after success
      shell: echo always_after_success
