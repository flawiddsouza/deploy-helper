- name: "{{ deployment_name }}"
  hosts: test
  vars_files:
    - provider: sops
      src: secrets-selector.enc.yml
    - provider: sops
      src: "{{ selected_vars_file }}"
  tasks:
    - name: "Use {{ secret_value }}"
      command: echo success
