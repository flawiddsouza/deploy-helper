- name: Deploy {{ env }}
  hosts: test
  vars:
    env: prod
  tasks:
    - name: Setup {{ env }}
      command: echo setup

    - name: Deploy {{ env }}
      command: echo deploy

    - name: Finalize {{ env }}
      command: echo finalize
