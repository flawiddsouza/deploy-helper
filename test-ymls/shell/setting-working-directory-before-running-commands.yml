---
  - name: Test
    hosts: test
    tasks:
      - name: Set vars
        chdir: tests/mocks/ls-dir
        command: ls

      - name: Set vars
        chdir: tests/mocks/ls-dir2
        shell: ls
