hosts:
  my_host:
    host: host ip
    port: 22
    user: username
    ssh_key_path: /home/username/.ssh/my_host
  local:
    host: localhost
  remote:
    host: localhost
    port: 2222
    user: root
    password: password
